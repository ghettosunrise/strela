/*
    Here must be containers
    How container is different from component?
    Try differing them by name: 
    If name is non-unique, like People of Cases
    instead of abstract like Button, Icon so it represents a set of components
    then it's a container
*/

export default from "./AgencyHeader"
